# PhotoBooth Application Dependencies
# Core image processing and computer vision
opencv-python>=4.5.0,<5.0.0
Pillow>=8.0.0,<11.0.0

# Printing support (CUPS bindings for Python)
pycups>=2.0.0,<3.0.0

# Note: tkinter is included with Python standard library
# Note: Other imports (threading, time, os, json, logging, datetime, shutil, pathlib) are built-in modules 